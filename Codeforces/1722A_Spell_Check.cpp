#include<bits/stdc++.h>
using namespace std;
int main()
{
     int t;
     cin>>t;
     while(t--)
     {
        int len;
        cin>>len;
        string s;
        for(int i=0;i<len;i++)
        {
            cin>>s[i];
        }
        cout<<f(s);
     }
}