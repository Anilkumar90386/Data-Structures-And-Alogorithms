#include<bits/stdc++.h>
using namespace std;
void f(int i,int arr[],vector<int>&ls,int n)
{
    if(i==n)
    {
        for(int i=0;i<ls.size())
    }
}
int main()
{
    int n;
    cin>>n;
    int arr[n];
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    vector<int>ls;
    f(0,arr,ls,n);
}