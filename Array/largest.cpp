#include<bits/stdc++.h>
using namespace std; 
int main()
{
    int n;
    cin>>n;
    int arr[n];
    int largest=0;
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
    }
   /* for(int i=0;i<n;i++)
    {
        largest=max(largest,arr[i]); TC -> O(N)
    }                                 SC -> O(1)
    cout<<largest;*/
   

}